<?php
/**
 * @file
 * Group all callback used to generate admin part of module.
 */

/**
 * Callback : return the admin configuration widget form.
 */
function auto_playlist_deezer_config_widget_form($form, &$form_state) {

  $test = 0;

  return $form;
}
